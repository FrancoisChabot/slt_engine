#ifndef SLT_RUNTIME_RENDER_BLENDING_H
#define SLT_RUNTIME_RENDER_BLENDING_H

#include <functional>
#include <memory>

namespace slt {
  namespace render {

    // TODO: add blend config once needed
    struct BlendingUsage {
      BlendingUsage();
      ~BlendingUsage();
    };
  }
}  // namespace slt
#endif
